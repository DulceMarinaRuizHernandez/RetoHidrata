services:
  - type: web
    name: hydroboost-app
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    rootDir: .
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: FLET_WEB_PORT
        value: $PORT