services:
  - type: web
    name: hydroboost-app
    runtime: python3
    buildCommand: |
      cd /opt/render/project/src || exit 1
      pwd
      ls -la
      if [ -f requirements.txt ]; then
        pip install -r requirements.txt
      elif [ -f mi_proyecto_flet/requirements.txt ]; then
        pip install -r mi_proyecto_flet/requirements.txt
      else
        echo "ERROR: requirements.txt not found in /opt/render/project/src or mi_proyecto_flet/"
        ls -la
        exit 1
      fi
    startCommand: python app.py
    rootDir: .
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: FLET_WEB_PORT
        value: $PORT